### Added

* WSL Windows setup now deploys `windows/powershell/Microsoft.PowerShell_profile.ps1` to the current Windows Documents folder, so it works whether `Documents` is local or redirected through OneDrive
