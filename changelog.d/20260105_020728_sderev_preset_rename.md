### Added
* WSL detection with automatic Windows config installation
* `--skip-windows` flag to skip Windows setup in WSL
* Windows Startup folder integration for AutoHotkey scripts
* Winget package installation support

### Changed
* Renamed `Profile` to `Preset` throughout codebase
* CLI flag `--profile` renamed to `--preset`
